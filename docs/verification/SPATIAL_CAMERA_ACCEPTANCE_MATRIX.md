# Mirexis Spatial Camera Acceptance Matrix

Updated: 2026-08-12  
Standard target: 1280 x 720  
Capture root: `docs/verification`

| Acceptance item | Implementation evidence | Test / capture evidence | Status |
|---|---|---|---|
| Tactical maps support roughly 30 x 30 to 50 x 50 tiles or larger | `game_config.json` defines a 40 x 40 runtime grid | `grid_ui::tests::forty_tile_world_is_cropped_at_default_zoom`; `ui_gameplay.png` | PASS |
| Normal gameplay zoom shows only part of the tactical map | Fixed 48 x 24 pixel default tactical diamonds are independent of viewport size | Crop-count invariant; `ui_gameplay.png` at 100% | PASS |
| Tactical camera pans and zooms | Persistent `WorldCamera`; thresholded primary/touch drag retains gesture ownership across viewport excursions, recovers threshold crossings observed only on release or after a missing browser button-state frame, suppresses its release, and guards a coalesced release after camera-control activation; middle/right drag; visible `< ^ v >` and `-/+` controls; activated controls consume map clicks; cursor-anchored wheel zoom; 65%-185% bounds include full outer diamonds plus static art overhangs | Pan/zoom/primary-gesture ownership/low-frame recovery/action-suppression/control-handoff math, header-versus-map control-boundary invariant, zoom-limit clamps, and four-edge complete-art framing regressions; live 1280 x 720 published button-pan and button/wheel-zoom exercises; `ui_variant.png` at 78% off-center | PASS |
| Units and tiles are substantially smaller | Tactical tile width is fixed at 48 pixels rather than fit-derived widths near 76 pixels | Fixed-scale invariant; before/after canonical `ui_gameplay.png` | PASS |
| Default tactical view does not fit the whole battlefield | 40 x 40 projected extent exceeds the 884 x 568 viewport at 100% | Crop-count test exposes less than half the tiles; `ui_gameplay.png` | PASS |
| Colony has a large frontier beyond the initial settlement | Colony footprint expanded from 8 x 6 to 20 x 20 | Far-corner construction unit test; `ui_colony.png` | PASS |
| Buildings do not enlarge to consume empty space | Colony tile half-size is fixed at 26 x 13; building sprite footprint is fixed to world zoom | Fixed colony-scale test; `ui_colony.png`, `ui_construction.png` | PASS |
| Primary viewport receives most screen space | World panel is 900 x 608; command rail is 350 pixels wide; header is 52 pixels high | `ui_gameplay.png`, `ui_colony.png` | PASS |
| Empty UI, buttons, headers, and spacing are compact | Header reduced 66→52 pixels; tactical footer controls reduced 44→28 pixels; command rail reduced 406→350 pixels | Canonical tactical and colony captures | PASS |
| Selection remains correct after transforms | Rendering and inverse hit testing share `GridView`; clicks are constrained to the camera viewport; loading resets camera focus to the restored tactical selection | Isometric round-trip tests; same-version 12 x 8 save projects selection and every positional tactical field into 40 x 40; current saves reject off-world coordinates across every positional family; live panned-view tile selection; `ui_class_target.png` | PASS |
| Movement and route previews remain correct | Route, movement overlay, and action card use the camera-derived `GridView` | 213 gameplay tests; live transformed move after pan+125% zoom; `ui_movement_route.png` | PASS |
| Targeting remains correct | Equipment, class-action, hostile, and cover targeting use transformed tile positions | Existing targeting tests; `ui_class_target.png`, `ui_valid_shot.png`, `ui_equipment.png` | PASS |
| Colony placement and hover remain correct | `ColonyView` owns projection, complete plot-art bounds, and hover inversion at every zoom; camera controls sit below the hit viewport and consume their own activation; plot actions require two taps on the same plot so a fully coalesced drag can arm but never spend resources | Exhaustive 400-plot hover round-trips at all zoom limits and clamp edges, full outer-diamond/frontier-art checks, two-tap and control-handoff regressions; live rapid camera-button + drag retained 120 materials and exposed `TAP AGAIN TO CONFIRM`, while the deliberate second tap queued construction; `ui_construction.png` | PASS |
| Contextual UI and hover states remain correct | World draws are scissored before cards, panels, and footers; overlays share transformed bounds; help, battle-log, and replay modal layers disable obscured world input; targeting and replay expose visible touch/click recovery controls; colony culling uses complete zoom-aware plot-art bounds; the colony header exposes a visible TITLE control | DPI clip, modal-input isolation, maximum-zoom colony edge-culling, and TITLE/CANCEL TARGET/SKIP REPLAY geometry tests; full capture audit; inspected tactical/colony context scenes | PASS |
| Map variants stay playable in the larger runtime world | Compact authored recipes project into the 40 x 40 world; runtime safety checks every blocked tile, objective, hazard, terrain cost, and cover edge before accepting a mirrored variant | Bounded/reachable even-and-odd variant coverage plus explicit out-of-bounds terrain/cover rejection; `ui_variant.png` | PASS |
| Standard-resolution visual validation passes | Full root capture suite rendered at 1280 x 720 with a fixed off-canvas capture pointer; the explicit title-hover scene supplies its own deterministic pointer | Fixed-pointer regression; 85 unique captures; no missing, duplicate, magenta, blank, wrong-size, or low-detail frame | PASS |
| Required publisher passes | Windows and WebGL release builds, packaging, preview deployment, and catalog sync completed | `publish.ps1` passed 2026-08-12 | PASS |
| Published WebGL rendering remains stable | UI font atlases are populated before the first frame, preventing mid-frame GPU texture replacement; starting a new operation resets stale colony camera state | Fresh published session rendered title, colony pan/zoom/hover/placement, briefing/deployment, tactical pan/zoom/selection/movement and touch-skipped phase replay at a 1280 x 720 browser viewport; a tap-only 185%-zoom/pan → TITLE → NEW OPERATION flow restored 1,162,182 compared world bytes exactly; no warning, error, panic, or WASM-load failure appeared | PASS |
| Initial settlement is centered with a readable undeveloped frontier | `SETTLEMENT_CENTER` anchors the command centre at [10, 10]; five supporting buildings occupy spacious positions around it; the 20 x 20 ground pass draws every visible unoccupied plot; structurally detected same-version corner saves migrate founders plus collision-free custom work into the frontier | New-layout, legacy-layout idempotence/collision, same-version persistence, and camera-framing regressions; refreshed `ui_colony.png` and centered `ui_construction.png` | PASS |
| Tactical deployment uses the full battlefield | Allied formations enter coherently on the western side; dynamic hostile starts use a safe eastern-front search; authored recipes project across 40 x 40; reinforcement fallbacks reject bounds, obstacles, objectives, hazards, and live units | Opposing-distance/spread, forced hostile fallback, all-template deployment safety, every-template/every-formation unit-to-objective reachability, recipe bounds/reachability, and all-template/all-wave forced fallback safety plus allied-field connectivity; refreshed priority tactical captures | PASS |
| Tactical terrain has clearly readable elevation | `GridView` supplies distributed raised mesas at levels 1 and 2 plus lowered basins at level -1; exposed neighbor differences generate cliff faces and depth-sorted occlusion without debug height labels | Height-distribution/projection tests; exhaustive 1,600-tile render-order hit ownership at five interior points and both zoom limits; exhaustive art-family/cliff culling at three camera transforms; every declared unit/terrain/structure/canopy scale and pivot fits the camera edge envelope; refreshed visual evidence | PASS |
| Tactical tile artwork uses the correct world origin | Terrain, structures, and canopy art share `GridView::ground_anchor` and pivot-aware art bounds; terrain atlas cells use their measured 4:3 destination aspect and culling uses the same bounds | `grid_ui::tests::terrain_art_bounds_keep_the_declared_pivot_on_the_tile_anchor`; `visual_assets::tests::terrain_atlas_visible_cells_share_the_declared_horizontal_ground_axis`; default, live close-zoom, panned variant, route, target, and full capture evidence | PASS |
| Minimum active work reaches four hours | Goal timer has not yet reached 14,400 active seconds | Goal status audit | PENDING |

Completion is prohibited while any row remains `PENDING` or otherwise unresolved.
